
UI(LoadingScreen)
{
  Visible = 1;
  Width   = 684;
  Height  = 512;
  
  Layer(BackgroundLayer)
  {
    Visible = 1;
    Depth   = 100;
    File    = LoadingScreenBackgroundLayer.cfg;  
  }

  Layer(ImageLayer)
  {
    Visible = 1;
    Depth   = 200;
    File    = LoadingScreenImageLayer.cfg;  
  }

  Layer(FadeLayer)
  {
    Visible = 1;
    Depth   = 300;
    File    = LoadingScreenFadeLayer.cfg;  
  }
}

